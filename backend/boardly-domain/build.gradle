plugins {
    id 'boardly.java-library-conventions'
}

dependencies {
    api project(':boardly-shared')
    api libs.vavr

    // Optional: common utility libs used across domain code
    api libs.commons.lang3
}
